/* eslint-disable react/prop-types */
export default function Main({ children }) {

    return (
        <main style={{
            position: "relative",
        }}>
            {children}
        </main>
    )
}